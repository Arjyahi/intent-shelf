# Demo Assets

This folder holds presentation assets for the public GitHub README.

Current files are SVG placeholders so the README layout is stable before final screenshots are captured. Replace them with real assets when you have polished demo imagery.

Recommended replacements:

- `home-feed-placeholder.svg` -> `home-feed.png`
- `product-detail-placeholder.svg` -> `product-detail.png`
- `feed-controls-placeholder.svg` -> `feed-controls.png`

If you swap in PNG files, update the README image links once and keep the naming consistent for future portfolio updates.
